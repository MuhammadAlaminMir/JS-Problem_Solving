var name = "alamin";

if (name.length > 4) {
    console.log("nam amer akib");
} else {
    console.log("ami alamin");
}

//when we didn't need to change the value of a var that time we use const
//or if we have to change it we use let

var name = "bangladesh";
//we can change var anytime

const name = "Bangladesh";
//if we declare const we can't change it from anywhere

let name = "Bangla";
//let is like var we can change it
